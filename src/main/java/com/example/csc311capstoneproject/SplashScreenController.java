package com.example.csc311capstoneproject;

import javafx.application.Application;
import javafx.stage.Stage;
/**
 * Entry point of the JavaFX application, responsible for launching the splash screen.
 * This class extends the Application class and overrides the start method to set up the primary stage.
 */
public class SplashScreenController extends Application {
    /**
     * Main method for launching the application.
     * This method is the entry point for the JavaFX application.
     *
     * @param args Command-line arguments passed to the application.
     */
    public static void main(String[] args) {
        launch(args);
    }
    /**
     * Starts the primary stage of the application.
     * This method is called after the application has been launched and is ready to start.
     *
     * @param primaryStage The primary stage for this application, onto which the application scene can be set.
     */
    @Override
    public void start(Stage primaryStage) {
// Implementation of the initial UI setup goes here
    }
}
